<%= form_with model: @supplier, local: true do |form| %>
 
  <% if @supplier.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@supplier.errors.count, "error") %> prohibited
        this supplier from being saved:
      </h2>
      <ul>
        <% @supplier.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <div class="">
    <p>
      <%= form.label :company %><br>
      <%= form.text_field :company, class: "" %>
    </p>
  </div>

  <%= form.submit class: "" %>

 
<% end %>